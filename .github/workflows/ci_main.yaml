name: Ingest Branch CI Pipeline

# Trigger strictly on pushes to the ingest branch
on:
  push:
    branches:
      - ingest

jobs:
  build-and-run:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Check out the repository code
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          ref: ingest

      # Step 2: Build the Docker image
      # Passes secrets/env vars as build arguments if needed during image creation
      - name: Build Docker Image
        run: |
          docker build -t train-ingest .

      - name: Tag Docker image
        run: |
          docker tag train-ingest priyan5hu24/train-ingest:latest
          
      # Step 3: Run the Docker container on the GitHub server
      # -d runs in detached mode, --name gives the container a predictable handle
      - name: Run Docker Container
        env:
          AWS_ACCESS_KEY: ${{ secrets.AWS_ACCESS_KEY }}
          AWS_SECRET_KEY: ${{ secrets.AWS_SECRET_KEY }}
          BUCKET: ${{ secrets.BUCKET }}
        run: |
          docker run -d --name ingest-app -p 3333:3333 -e AWS_ACCESS_KEY="$AWS_ACCESS_KEY" -e AWS_SECRET_KEY="$AWS_SECRET_KEY" -e BUCKET="$BUCKET" priyan5hu24/train-ingest:latest


      # Step 4: Check container health and view logs
      - name: Verify Container Execution
        run: |
          docker ps -a
          echo "=== Container Logs ==="
          docker logs ingest-app